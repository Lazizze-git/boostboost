
  # Profile editing app

  This is a code bundle for Profile editing app. The original project is available at https://www.figma.com/design/qldIPn2zGUzUTB251VN1VZ/Profile-editing-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  